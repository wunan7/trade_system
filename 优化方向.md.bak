一、信号层创新（新增信号源 / 改进现有信号）

  1. 宏观择时信号 — Market Regime Detector

  现状缺失：当前 7 个信号源全部是个股维度，没有大盘/宏观层面的判断。牛市中 D 级股可能也涨，熊市中 A+ 级也可能跌。

  思路：新增第 8 个 Adapter，采集宏观信号（10Y国债利率、M2增速、沪深300波动率VIX、北向资金流向、融资余额变化率），输出
  market_regime = risk_on / neutral / risk_off。在 risk_off 状态下全面降低仓位系数，risk_on 时放宽买入阈值。

  ---
  2. 行业轮动策略 — Sector Rotation

  现状缺失：TrendRadar 只做了板块舆情的多空判断，没有行业间的相对强弱和轮动逻辑。

  思路：计算各行业过去 N 天的相对强弱（RS）排名，结合行业估值分位数（行业PE百分位）和资金流入排名，构建行业动量+价值打分
  。策略层面优先从"动量上升 + 估值尚可"的行业中选股，回避"动量下行 + 估值高位"的行业。这相当于在个股筛选之上加了一层行业
   Beta 筛选。

  ---
  3. 事件驱动信号 — Event-Driven Adapter

  现状缺失：系统是定期评级（日频/月频），没有对突发事件的响应能力。

  思路：新增事件信号源，监控：财报发布后的业绩超/低预期（实际 EPS vs 一致预期）、股东增减持公告、重大合同/并购公告、股权
  激励行权条件达成。事件触发后生成临时信号，可绕过月度检查点直接影响评级或触发预警。

  ---
  4. 资金微观结构信号 — Flow & Microstructure

  现状缺失：当前无资金面信号，缺少筹码分布、主力行为分析。

  思路：利用 Level-2 分时数据或日频大单数据，计算：主力净流入/流出占比、大单成交占比变化趋势、换手率异动（vs
  20日均值）、龙虎榜机构席位净买入。输出 flow_bullish / flow_bearish，可作为三重共振的第四维度验证，或替代当前舆情维度。

  ---
  二、评级与共振层创新

  5. 动态自适应权重 — Adaptive Signal Weighting

  现状局限：信号权重是策略文件中的静态值（valuation 30%, screener 25%...），无论市场环境如何不变。

  思路：基于滚动窗口（如过去 6 个月）统计每个信号源的历史预测准确率（信号为 bullish 后 N 天的实际收益），动态调整权重——
  近期预测准确的信号源获得更高权重，失灵的信号源自动降权。可用简单的指数加权平均实现，不需要复杂ML。

  ---
  6. 多层级共振升级 — N-Dimensional Resonance

  现状局限：三重共振只区分"触发/不触发"的二元结果，丢失了共振强度信息。

  思路：
  - 将共振从二元升级为强度评分：每个维度不只看 bullish/bearish，而是看 score 的绝对值和 confidence
  的乘积。三维得分的几何平均值作为共振强度（0-100）。
  - 增加时间一致性维度：不只看当天信号，还看过去 N 天的信号方向一致性（连续 3 次 bullish 强于单次 bullish）。
  - 共振强度直接映射到仓位大小，取代当前简单的 resonance_boost = 1.5 固定倍数。

  ---
  7. 反向信号利用 — Contrarian / Mean Reversion

  现状局限：系统是纯顺势逻辑（看多买入、看空卖出），没有均值回归或反转策略。

  思路：当某只股票的 valuation 信号极度看多（严重低估）但 chan
  信号看空（技术面弱势）时，当前系统因为技术面不达标而过滤掉。但这恰恰是经典的"价值陷阱 vs
  黄金坑"场景。新增一个反转共振策略：价值面极度看多 + 技术面超卖（而非看多）+ 波动率触顶回落 →
  视为潜在反转买点，小仓位试探。

  ---
  三、仓位与风控层创新

  8. 组合优化 — Portfolio-Level Optimization

  现状局限：仓位计算是逐只股票独立进行的（position_pct = risk_limit × rating_coeff），没有考虑持仓间的相关性。

  思路：在个股仓位计算之后，增加一层组合优化：
  - 计算持仓股票间的相关性矩阵（基于历史收益率）
  - 用 Mean-Variance 或 Risk Parity 模型约束总组合风险
  - 高度相关的持仓自动降低其中一只的仓位
  - 设定行业集中度上限（如单一行业不超过总仓位 30%）

  这样从"个股最优"升级到"组合最优"。

  ---
  9. 动态止损止盈 — Adaptive Exit Rules

  现状局限：止损止盈是固定百分比（-10% / +30%），不区分高波动股和低波动股。

  思路：
  - ATR 自适应止损：止损幅度 = N × ATR（平均真实波幅），高波动股自动放宽止损，低波动股收窄止损，避免被正常波动震出。
  - 移动止盈（Trailing Stop）：不设固定止盈点，而是从最高点回撤 N% 时止盈。让利润奔跑。
  - 时间止损：持仓超过 M 个月仍无盈利且评级未提升，自动减仓——释放资金给更有机会的标的。

  ---
  10. 分批建仓 / 分批出场 — Scaling In/Out

  现状局限：买卖是一次性操作——达到买入条件时按目标仓位一次性买入，卖出时一次性清仓。

  思路：
  - 分批建仓：首次买入 1/3 仓位，若下一个检查点仍为 A 级且价格未大幅上涨，加仓
  1/3，再次确认后满仓。降低在高点一次性重仓的风险。
  - 分批止盈：盈利 15% 减仓 1/3 锁定利润，剩余仓位用 trailing stop 跟踪。
  - 评级渐变响应：从 A 降到 B 时减仓 50%（而非等到降到 C/D 才卖），从 B 升到 A 时加仓 50%（而非一次满仓）。

  ---
  四、回测与执行层创新

  11. 多周期检查点 — Multi-Frequency Rebalancing

  现状局限：回测只支持月度检查点（checkpoint_frequency: monthly），错过月内的重大变化。

  思路：
  - 支持周频甚至日频检查点，但代价是预计算数据量大增
  - 折中方案：常规月度调仓 + 事件触发调仓（日频扫描，只有当出现三重共振触发或止损触发时才执行调仓）
  - 不同信号源用不同频率：缠论日频扫描、估值月频更新、财务季频更新，各自在适合的频率上刷新

  ---
  12. 策略组合与对冲 — Strategy Ensemble

  现状局限：每次回测只跑单一策略（default/aggressive/conservative），没有策略间的组合。

  思路：
  - 多策略并行：资金分配给多个子策略（如 40% default + 30% conservative + 30% 行业轮动），各自独立运行
  - 策略间对冲：当 default 策略满仓做多时，如果 Market Regime 为 risk_off，用一部分资金配置在 conservative 策略或反向
  ETF 上
  - 动态分配：基于每个子策略近 6 个月的夏普比率动态调整资金分配比例

  ---
  13. 机器学习信号融合 — ML-Based Meta Model

  现状局限：信号融合是固定公式（加权平均），无法捕捉信号间的非线性交互。

  思路：
  - 用现有 7 个信号的历史数据（score + signal + confidence）作为特征，未来 N
  天收益率作为标签，训练一个轻量级模型（XGBoost / LightGBM）
  - 模型自动学习："当估值看多 + 缠论中性 + 舆情看多时的组合效果"这类非线性模式
  - 用滚动训练窗口避免前视偏差，与现有加权评分做 ensemble 或直接替代
  - 保留可解释性：用 SHAP 值输出每个信号的贡献度，仍可在简报中展示

  ---
  14. 多市场/多资产扩展 — Cross-Asset Strategy

  现状局限：系统完全聚焦 A 股个股，没有其他资产类别。

  思路：
  - 股债轮动：根据宏观择时信号（思路1），在 risk_off 时自动将部分仓位从 A 股转移到国债 ETF（如 511010）
  - 大小盘轮动：监控沪深300/中证1000的相对强弱，动态切换大盘价值股和小盘成长股的选股池
  - 港股/A股联动：AH 溢价率极端时，A 股高溢价标的发出 bearish 修正信号

  ---
  15. 情景压力测试 — Scenario Stress Testing

  现状局限：回测只测历史已发生的场景，没有假设性压力测试。

  思路：
  - 定义极端情景：2015 股灾（-40%）、2018 贸易战（-25%）、2020 疫情（-15% 后 V 型反弹）、利率突然上行 200bp
  - 用历史同类时期的市场数据做蒙特卡洛模拟，测试当前持仓在极端场景下的最大损失
  - 输出：每个情景下的组合回撤、单只最大亏损、需要多少天恢复
  - 可作为每日简报的附加板块："压力测试：当前组合在 2015 股灾场景下预计回撤 -22%"

  ---
  总结：优先级建议

  ┌────────┬───────────────────┬────────────────────────────────────┐
  │ 优先级 │       方向        │                理由                │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P0 高  │ 5. 动态自适应权重 │ 实现简单，直接提升现有评级准确率   │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P0 高  │ 9. 动态止损止盈   │ 现有固定止损是回测收益的主要拖累   │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P0 高  │ 1. 宏观择时信号   │ 解决"系统性风险无法规避"的核心短板 │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P1 中  │ 8. 组合优化       │ 从个股最优到组合最优的质变         │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P1 中  │ 11. 多周期检查点  │ 月度频率太低，错过很多机会         │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P1 中  │ 10. 分批建仓/出场 │ 降低择时风险，提升实盘可操作性     │
  ├────────┼───────────────────┼────────────────────────────────────┤
  │ P2 低  │ 13. ML信号融合    │ 潜力大但需要足够历史数据验证       │
  └────────┴───────────────────┴────────────────────────────────────┘

  这些方向可以渐进式实现——每个都是在现有 Adapter + Strategy JSON 架构上的扩展，不需要推翻重来。